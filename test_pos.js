const email = "caixa@lamore.com.br";
const password = "admin"; // I don't know the password, I will just use fetch against the API to get a session if possible, wait, I can't.

// Let's just create a test route in the app that dumps headers and session.
